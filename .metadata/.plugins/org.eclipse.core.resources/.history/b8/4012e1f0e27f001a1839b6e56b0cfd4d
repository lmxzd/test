package test;

public class DemoHuWei {
	/**
	 * 进行字符串的反转，以空格为隔离
	 * @param a
	 * @return
	 */
	public static String reverseString(String a) {
		String[] arr = a.split("");
		for (int i = 0; i < arr.length; i++) {
			
		}
		return null;
		
	}
}
